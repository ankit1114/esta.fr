{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}
{% block paragraph %}
  {% block content %}
    <div class="QuellesDiv">
      <div class="container">
        <h2> {{ content.field_heading }} </h2>
        {{ Question }}
        {{ Answer }}
      </div>
    </div>
  {% endblock %}
{% endblock paragraph %}


