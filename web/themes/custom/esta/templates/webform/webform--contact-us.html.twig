{#
/**
* @file
* Theme implementation for a 'webform' element.
*
* This is an copy of the webform.html.twig theme_wrapper which includes the
* 'title_prefix' and 'title_suffix' variables needed for
* contextual links to appear.
*
* Available variables
* - attributes: A list of HTML attributes for the wrapper element.
* - children: The child elements of the webform.
* - title_prefix: Additional output populated by modules, intended to be
*   displayed in front of the main title tag that appears in the template.
* - title_suffix: Additional output populated by modules, intended to be
*   displayed after the main title tag that appears in the template.
*
* @see template_preprocess_webform()
* @see _webform_form_after_build()
*
* @ingroup themeable
*/
#}

<form {{ attributes }}>
  {{ title_prefix }}

  <div class="form-group">
    <label for="usr">Votre nom*</label>
    {{ element.elements.votre_nom }}
  </div>
  <div class="form-group">
    <label for="usr">Votre adresse électronique*</label>
    {{ element.elements.votre_adresse_electronique }}
  </div>
  <div class="form-group">
    <label for="usr">Numéro de téléphone</label>
    {{ element.elements.numero_de_telephone }}
  </div>
  <div class="form-group asisdiv">
    <label for="usr">Veuillez sélectionner la catégorie la plus appropriée à votre question*</label>
    <div class="row">
      <div class="col-md-6">
        <div class="radio">
          {{ element.elements.veuillez_selectionner_la_categorie_la_plus_appropriee_a_votre_qu }}
        </div>
      </div>
    </div>
    {{ element.elements.actions }}
  </div>
  {{ element.form_build_id }}
  {{ element.form_token }}
  {{ element.form_id }}
  {{ element.actions }}
  {{ title_suffix }}
</form>

